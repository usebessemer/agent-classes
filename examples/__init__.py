"""Runnable examples for the Bookkeeper framework.

A package marker so the CI test can import the quickstart and prove it still
runs; the examples themselves are standalone scripts (`python examples/quickstart.py`).
"""
